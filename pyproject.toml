[project]
name = "meeting-intel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "structlog>=25.5.0",
]

[tool.uv.workspace]
members = [
    "backend",
]
